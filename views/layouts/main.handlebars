<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Xdream</title>
</head>
<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/"> Xdream</a>
            </h1>
            <nav>
                {{#if loggedIn}}
                <a href="/dashboard">
                <button id="dashboard">Dashboard</button></a>
                <a href="/dream-forum">
                <button id="dreamscape-forum">Dreamscape Forum</button></a>
                <button id="logout">Logout</button>
                {{else}}
                <a href="/login">Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>

    </div>
    {{#if loggedIn}} 
    <script src="/js/logout.js"></script>
    <script src="/js/adding-dream.js"></script> 
    {{/if}}
</body>
</html>

